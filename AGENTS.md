# AutoTrade 协作规则

> 本项目使用 Project Cairn 组织项目知识：`AGENTS.md` 是规则与导航入口，`cairn/` 是项目知识/状态层。
> 同目录的 `CLAUDE.md` 只含一行 `@AGENTS.md`，使 Claude Code 读取同一份规则；Codex 直接读本文件。

## 项目一句话

TradingView 自动交易系统：FastAPI 后端 + Vue3 前端，多券商（Paper/富途/IBKR）、股票 + 期权（含卖方）、回测/选股/风控/通知全流程，中英双语 Web 管理后台。

> 本文件由 `cairn init` 生成，已填入本项目自身的定位与 provider 配置；其他项目复用前应各自运行 init。

## 初始化配置

- Graduation provider(s)：暂未对接（暂缓对接——首次毕业时再连接知识库）
- 知识库索引：尚未配置
- 毕业目标：尚未配置

## 进入项目后的阅读顺序

1. 先读本文件（AGENTS.md）。
2. 读 `cairn/LOG.md` 最新几条（新条目在最上）了解近期进展与关键决策。
3. 按任务需要读相关的 `cairn/` 知识专题文档。
4. 工程文档地图见 `README.md` 的「文档」表（架构/策略开发/期权/FAQ 等）；产品规划见根目录 `ROADMAP.md`（新需求先对照其中「明确不做」）。

## 文档职责

| 文件 | 角色 | 维护方式 |
|---|---|---|
| `AGENTS.md`（根目录） | 规则与导航 | 很少变动 |
| `CLAUDE.md`（根目录） | 一行 `@AGENTS.md` 桩文件 | 写一次，不再动 |
| `cairn/LOG.md` | 时间顺序日志 | 新条目加在顶部（最新在前），每条 ≤ 20 行，只写摘要 + 指针 |
| `cairn/<topic>.md` | 知识专题文档（当前真相） | 就地更新；坑点写入正文小节并用 `contains` 标注；修订在 LOG 留指针 |
| `cairn/Reference/` | 外部原始输入 | 按需创建；只增不改 |
| `cairn/Cited.md` | 知识库引用清单 | 只存指针，绝不复制原文 |

> 其余文件只在具体信号出现时创建（要记录的决策、被解决的坑、跨会话的目标），不预建空壳。工程资产（代码/配置/规格）不归本体系管理，留在代码树里；`CHANGELOG.md`/`ROADMAP.md` 是产品文档，不迁入 `cairn/`。

## 冲突仲裁规则

- 优先级：**知识专题文档 > LOG 历史**；规则层面的冲突以本文件为准。
- 业务/设计结论以 `cairn/` 知识专题文档的最新记录为准，而非更早的 LOG 条目。

## 知识库消费反射

- 在开展"其可复用内核（产出或依赖的任何结论）够格毕业"的工作前，先查本项目自己的 `cairn/` 知识专题文档；目前尚未对接外部知识库（provider 暂缓对接——见上文初始化配置），外部索引检查与 `cairn/Cited.md` 引用在对接后启用。

## 文档协作规则

- 改动前先判断用户要的是"讨论/建议"还是"直接改文档"；对方说"先看看/先评估"时，先给分析——不要直接重写正式文档。
- 纠正过往判断时追加更正说明，不要静默覆盖。
- 未经确认的判断不写成定论。

## 知识沉淀规则

- 每个实质性进展后，在 `cairn/LOG.md` 顶部加一条（摘要 + 指针）；结论沉淀进 `cairn/` 知识专题文档。
- **完成答复门禁**：在任何完成声明之前——包括但不限于工作完成/已实现/已定稿/已更新/已同步/已验证/测试通过、问题已修复/已解决、交付物可用、声明工作结束及语义等价表述——先执行 `references/maintenance.md` 的 Cairn 检查点；只更新其触发矩阵要求的记录并核实后再答复。明确的只读/勿改请求禁止 Cairn 写入。
- 跨项目可复用的经验在对接知识库后经毕业机制沉淀（provider 暂缓对接——见上文初始化配置）。

## 工程硬性约定（改代码前必须知道）

- **注释/文档/提交信息用中文**；代码标识符用英文。
- **风控 fail-closed**：风控路径上任何异常必须落在"拒单"分支，绝不允许异常放行。改 `risk/` 时保持此不变式。
- **Alembic 迁移幂等**：新迁移沿用 guard 模式（先查列/表存在性）；`0001` 基线是 `create_all`，新表/新列要同时改 `db/models.py` 和新迁移文件。
- **期权符号** `US.AAPL|20250919|C|230`：保留市场前缀（`split(".", 1)[0]` 路由依赖）；乘数全链路传递（OrderRequest→Order→Position→风控→盈亏）。
- **信号幂等**：一切自动下单入口都要有 dedup_key；本地策略去重键含最后一根 K 线日期。
- **前端 i18n**：中文原文即词条 key，模板 `$t('中文')`、脚本 `tr('中文')`；新增文案在 `frontend/src/i18n.js` en 词典补英文；en 值禁用 `{ } | @`。
- **策略回测/实盘同码**：`on_bar`/`on_rebalance` 两用；期权 `on_run` 实盘专属（async），回测 API 对期权策略显式报错。
- **期权策略单轮单向**：同一次 on_run 不得既买回又开新仓（持仓同步竞态）。

## 常用命令与测试注意

```bash
cd backend && python -m pytest tests -q    # 后端全量测试（须在 backend/ 下运行，当前 145 个）
cd frontend && npm run build               # 改前端后必须构建通过
uvicorn app.main:app --app-dir backend --host 0.0.0.0 --port 8000   # 本地起服务（先 cp .env.example .env）
```

- 测试自带 SQLite 临时库（0001 基线建表），券商用 stub；yfinance/akshare 全 mock，勿写真实网络测试。
- Paper 撮合"先发成交回报再更新持仓"，依赖此顺序的测试勿改次序。

## 交付流程

改动完成的定义：后端 pytest 全绿 + 前端构建通过 + 相关文档同步（尤其 `docs/architecture.md` 的规则数/策略清单等易漂移处）+ README 功能表按需补行 + 本文件「知识沉淀规则」的完成答复门禁。演示用 `data/`、`.env` 不入库（已 gitignore）。

# Project Cairn 日志

本文件按倒序记录实质性进展——最新条目在最上、紧贴本行之下。每条保持简短——只写摘要与指针；结论沉淀进 `cairn/<topic>.md`。

## 2026-09-11 · 迭代14：路线图「近期」层全部落地

- 5 项一次做完：通知渠道 4→8、指标 9→15、日志筛选+CSV 导出、参数扫描热力图、期权链自动刷新+价差着色。
- 测试 145→161；前端构建通过；Playwright 实机验证全部 5 项（含热力图渲染、自动刷新计数、定时器清理）。
- **两个坑已固化进测试**（详见 `docs/architecture.md` 迭代14 节）：
  1. SQLAlchemy 的 SQLite 绑定**丢弃时区偏移而不换算**——筛选参数传 `+08:00` 会静默偏差 8 小时；
     修复是在 `_parse_dt` 统一换算成 UTC naive。回归测试特意选了能区分两种实现的临界点
     （早先写的断言两种实现都通过，抓不住 bug，已改）。
  2. 导出路由必须声明在 `/{id}` 之前，否则 `export.csv` 被当作 int 路径参数解析。
- 沙箱内 yfinance 被出口代理屏蔽，回测拿不到真实数据；热力图与期权链改用"预置数据 + Playwright 路由 mock"
  走真实渲染路径验证——这是本环境验证前端图表的可行姿势。
- ROADMAP 近期层已全部勾选，剩余为中期（付费数据源→期权回测→组合腿单）与远期。
- 提交 `1871703`，已合并至 main。

## 2026-08-16 · Project Cairn 初始化

- 初始化 Project Cairn 结构（git_policy: track；provider 暂缓对接；语言中文）。
- 历史迁移模式：`inventory_only`。
- 原 `CLAUDE.md` 的工程规则并入 `AGENTS.md`（「工程硬性约定」等节），`CLAUDE.md` 改为一行 `@AGENTS.md` 桩。
- 详见 `AGENTS.md` 与 `.cairn/config.yaml`。
- 本次初始化随提交 `b41431f` 合并至 main。（注：此条与下方盘点条目原先都写了"当前 main 位置"，
  属于会随每次合并过期的写法——日志记录当时事实即可，当前分支状态查 git，不在此处维护。）

## 2026-08-16 · 历史盘点（inventory_only）

- 初始化前项目已有 13 轮迭代（MVP → 期权 → 内置策略扩充 → 双语 GUI → 文档补全），演进史见 `CHANGELOG.md`，逐迭代设计决策见 `docs/architecture.md` 各章节。
- 既有知识载体盘点：`docs/` 7 份专题文档（架构/策略开发/期权/后台手册/FAQ/券商/TV 接入）、`CHANGELOG.md`、`ROADMAP.md`（产品路线图，含「明确不做」边界）、后端 85% 模块含中文 docstring。
- 上述文档保持原位不迁入 `cairn/`（工程/产品文档归代码树管理）；`cairn/` 从零开始，仅记录此后的协作知识。
- 当前状态：后端 145 测试全绿；main 与开发分支同步于「新增 ROADMAP.md」提交。

# 路线图

规划中的方向，按优先级分层。已完成的演进史见 [CHANGELOG.md](CHANGELOG.md)；当前版本的明确范围外声明见 `docs/architecture.md` 的「已知取舍」。本文件是意向清单，不是承诺——条目会随实际使用反馈调整。

## 近期（补短板）

- [ ] **更多通知渠道**：飞书、Discord、Bark（iOS 推送）、Server酱——`notify/base.py` 单方法接口，每个约半小时工作量
- [ ] **更多技术指标**：KDJ、OBV、VWAP、布林带宽等，注册进 `screener/indicators.py` 白名单（选股表达式与策略同时受益）
- [ ] **期权链页自动刷新报价**：当前为手动加载，可加定时刷新与买卖价差高亮
- [ ] **回测参数扫描结果可视化**：参数-收益热力图（当前为表格排名）
- [ ] **审计/信号日志的检索增强**：按标的/策略/日期组合筛选与导出

## 中期（增能力）

- [ ] **付费数据源接入**：Polygon（美股）、Tushare Pro（A股）——`DataProvider` 抽象已就绪，加 provider 类并注册即可；解锁分钟级长历史回测与更可靠的盘中数据
- [ ] **期权回测**：依赖上一项拿到期权历史数据后启动；回测引擎需扩展乘数撮合与到期结算
- [ ] **组合腿单**：价差（vertical spread）、跨式（straddle）等多腿策略——需要券商侧组合单支持与风控的组合保证金逻辑
- [ ] **希腊字母/IV 展示**：期权链页与持仓页显示 Delta/Theta/IV（IBKR 可直接取，富途看行情权限）
- [ ] **守护策略增强**：分批止盈、时间止损（持仓超 N 天离场）、按 ATR 的动态止损

## 远期（新方向）

- [ ] **ML 策略工作流**：按需 GPU 训练调度（RunPod API）+ 模型版本管理页 + ONNX CPU 推理接入策略框架
- [ ] **加密货币市场**：7×24 交易、ccxt 适配层——符号路由与时段校验需泛化
- [ ] **多用户与权限**：当前定位自托管单用户；如需团队使用要加角色、策略隔离与编辑器沙箱（exec 安全模型需重做）
- [ ] **英文文档**：GUI 已双语，文档体系视开源/分享需求跟进

## 明确不做

- **高频/低延迟交易**：架构定位是分钟级以上的信号驱动交易，不追求微秒级撮合
- **tick 级行情存储**：数据体量与免费源能力都不匹配
- **托管多租户 SaaS**：安全模型（策略代码 exec、密钥管理）按自托管设计

## 参与方式

想推进某一项：开 issue 讨论方案，或直接按 `CLAUDE.md` 的约定提 PR（后端测试全绿 + 前端构建通过 + 文档同步）。
